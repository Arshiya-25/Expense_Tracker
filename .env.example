# .env — NEVER commit this file to GitHub. Add it to .gitignore.
# These are secret values your app needs but shouldn't be public.

# Get this from mongodb.com → Create cluster → Connect → Drivers
MONGO_URI=mongodb+srv://arshiya5280_db_user:5gCtEsFXLrUGgJhP@cluster0.z92zbes.mongodb.net/finflow?retryWrites=true&w=majority
# JWT_SECRET is any long random string you make up — like a secret stamp
# The longer and more random, the more secure
JWT_SECRET=finflow_super_secret_12345_random_string

# Where your React app runs (for CORS)
CLIENT_URL=http://localhost:5173

PORT=5000
